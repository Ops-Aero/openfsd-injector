# Injector only. Do not copy opsaero-main/.env — that is Laravel/MySQL.
# These defaults match a container on the same host as opsaero-main.

OPENFSD_HOST=host.docker.internal
OPENFSD_PORT=6809
OPENFSD_API_BASE=http://host.docker.internal:8010
OPENFSD_CID=1
OPENFSD_PASSWORD=opsaeroadmin
